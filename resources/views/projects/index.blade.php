@extends('layouts.app')
@section('title', 'Project | Brillian Ghulam')
@section('content')
<section class="page-hero"><div class="shell relative z-10 text-center"><p class="text-sm font-semibold text-orange-300">Home <span class="mx-2 text-white/30">/</span> Projects</p><h1 class="mx-auto mt-5 max-w-4xl break-words text-4xl font-extrabold leading-tight tracking-[-.04em] sm:text-5xl lg:text-6xl">Project & Case Studies</h1><p class="mx-auto mt-5 max-w-2xl text-sm leading-7 text-slate-300 sm:text-base">Masalah, proses analisis, rancangan solusi, dan hasil implementasi dalam satu alur yang mudah dipahami.</p></div></section>
<section class="section" x-data="{ filter: 'all' }"><div class="shell"><div class="flex flex-wrap justify-center gap-2"><button @click="filter='all'" :class="filter==='all' ? 'bg-cyan text-white border-cyan' : 'bg-white dark:bg-transparent'" class="rounded-full border border-slate-200 px-5 py-2.5 text-xs font-bold transition dark:border-slate-700">SEMUA</button>@foreach ($categories as $category)<button @click="filter='{{ $category->slug }}'" :class="filter==='{{ $category->slug }}' ? 'bg-cyan text-white border-cyan' : 'bg-white dark:bg-transparent'" class="rounded-full border border-slate-200 px-5 py-2.5 text-xs font-bold transition dark:border-slate-700">{{ strtoupper($category->name) }}</button>@endforeach</div><div class="mt-12 grid gap-7 lg:grid-cols-2">@foreach ($projects as $project)<div x-show="filter==='all'||filter==='{{ $project->category->slug }}'"><x-project-card :project="$project" :number="$loop->iteration" /></div>@endforeach</div></div></section>
@endsection
