@extends('layouts.admin')
@section('title','Profile')
@section('content')<div class="mb-8"><p class="kicker">Identity</p><h1 class="text-3xl font-bold">Profile & CV</h1></div><form method="POST" action="{{ route('admin.profile.update') }}" enctype="multipart/form-data" class="max-w-4xl bg-white p-6 shadow-sm sm:p-8">@csrf @method('PUT')<div class="grid gap-5 sm:grid-cols-2"><div><label class="label">Nama</label><input class="field" name="name" value="{{ old('name',$profile->name) }}" required></div><div><label class="label">Professional title</label><input class="field" name="title" value="{{ old('title',$profile->title) }}" required></div><div class="sm:col-span-2"><label class="label">Tagline</label><textarea class="field" name="tagline" rows="2" required>{{ old('tagline',$profile->tagline) }}</textarea></div><div class="sm:col-span-2"><label class="label">Professional summary</label><textarea class="field" name="summary" rows="6" required>{{ old('summary',$profile->summary) }}</textarea></div>@foreach (['location'=>'Lokasi','email'=>'Email','phone'=>'Telepon','linkedin_url'=>'LinkedIn URL','github_url'=>'GitHub URL','whatsapp_url'=>'WhatsApp URL'] as $name=>$label)<div><label class="label">{{ $label }}</label><input class="field" name="{{ $name }}" value="{{ old($name,$profile->{$name}) }}"></div>@endforeach<div><label class="label">Foto profile (max 3 MB)</label><input class="field" type="file" name="photo" accept="image/*"></div><div><label class="label">CV PDF (max 5 MB)</label><input class="field" type="file" name="resume" accept="application/pdf">@if($profile->resume_path)<p class="mt-2 text-xs text-emerald-600">CV aktif tersedia.</p>@endif</div></div><button class="btn-primary mt-8">Simpan perubahan</button></form>@endsection
